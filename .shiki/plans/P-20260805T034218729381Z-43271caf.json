{
  "completion_conditions": [
    "A task that declares locks and has no granted lock record cannot be dispatched, and the refusal names the task and the missing locks.",
    "A task whose lock record exists and is active dispatches exactly as today.",
    "A task with an empty locks list dispatches exactly as today.",
    "`shiki lock acquire` failing is impossible to miss: a conflicted acquire is refused downstream even when its exit code is ignored.",
    "The full suite and the five dispatch-driving shell suites stay green."
  ],
  "grill_with_docs": {
    "decisions": [
      "The write-side hole is exact. `cmd_lock_acquire` (`scripts/shiki_tasks.py:946`) returns 1 at the conflict branch BEFORE `write_json(lock_file, record)` at `:966` and before `task[\"status\"] = status_after_lock_acquire(...)` at `:982`. So a conflicted acquire leaves no record and no status change \u2014 and reports it only through the exit code and a `locks_granted: false` field in its JSON.",
      "Nothing downstream re-checks. `dispatch_runner_task` (`scripts/shiki_runtime.py:512-530`) validates runtime assignment, `status in {ready, running}`, required tooling, auth, the handoff file, the worktree record and the session lease \u2014 and never reads `.shiki/locks/`. `cmd_task_status` (`scripts/shiki_tasks.py:1164`) writes `status = \"ready\"` with no lock check at all, and `shiki_cli.py:518` exposes `ready` as a free choice.",
      "Reproduced live 2026-08-04: an operator piped `shiki lock acquire` through `tail -1`, saw the closing brace, and missed `locks_granted: false` with five conflict reasons naming a superseded task. Handoff, status and dispatch all proceeded; the implementation was written, reviewed, merged, and only the closeout surfaced anything \u2014 as an unexplained Guardian requirement, because the ADR 0017 exemption needs the lock to move active -> released and there was no lock to move.",
      "The merge-side backstop is DELIBERATELY out of scope. Requiring a merged PR to show a lock grant would also add `scripts/mergegate_check.py` and four shell fixture files whose expect-ready fixtures never write a lock record \u2014 and it would retroactively block reopened work: `origin/main` currently holds exactly ONE active lock record for TWELVE non-terminal tasks. Dispatch-side only also removes the `mergegate_check.py` collision with the migration-mode task.",
      "LOCK-SUFFICIENCY CHECK (2026-08-04). MUST-CHANGE: `scripts/shiki_runtime.py` \u2014 the precondition goes beside the existing status check at `:522-526`, and `dispatchable_task_ids` (`:291-310`) plus the `cmd_runner_execute` status write (`:299`) must agree with it; `scripts/shiki_tasks.py` \u2014 `cmd_lock_acquire` (`:946`) and `cmd_task_status` (`:1164`); `tests/test_lock_lifecycle.py` \u2014 already drives `cmd_dispatch_check` at `:287` and would drift; `tests/test_dispatch_lock_precondition.py` \u2014 new. `shiki_runtime.py` already imports from `shiki_tasks`, so the shared helper introduces no cycle.",
      "VACUITY CAVEAT, and it must be stated in the code: a task with an EMPTY `locks` list has nothing to grant, so the precondition must return satisfied for it. Treating an empty list as ungranted would block every lock-free task.",
      "MUST-NOT-BREAK (verification command, not locks): `scripts/test_shiki_runner_claude.sh`, `scripts/test_shiki_daemon_runner.sh`, `scripts/test_shiki_loop_lock_guard.sh`, `scripts/test_shiki_control_plane.sh` and `scripts/test_shiki_goal_loop.sh` all dispatch or drive tasks in fixtures; the audit measured the dispatch-side precondition as breaking none of them, but they gate the change.",
      "Risk is high because this can refuse a dispatch. A wrong predicate stops all autonomous work, which is worse than the defect it fixes \u2014 so the failure direction matters: an unreadable or missing lock record for a task that DECLARES locks must refuse, and every refusal must name the task and the missing or conflicting locks."
    ],
    "source": "Reproduced live on 2026-08-04 and confirmed by reading the dispatch path; lock set computed by the repository-wide audit 2026-08-04.",
    "status": "complete"
  },
  "id": "P-20260805T034218729381Z-43271caf",
  "ingested_at": "2026-08-05T03:42:18+00:00",
  "non_goals": [
    "A merge-side lock-grant requirement in mergegate_check.py \u2014 it would retroactively block twelve non-terminal tasks that have no lock record on the default branch.",
    "Changing what a lock conflict means, or how locks are declared or released.",
    "Adding a `shiki lock release` command \u2014 a separate gap, recorded not fixed here.",
    "Auto-acquiring locks as a side effect of dispatch: dispatch must REFUSE, never grant.",
    "Changing the loop's decision function.",
    "Touching any file outside the declared locks."
  ],
  "outcome": "A task can no longer be dispatched, implemented and merged while its lock grant does not exist. Today `shiki lock acquire` returns a non-zero exit and writes NOTHING when the locks conflict, and every downstream step \u2014 handoff, status, dispatch \u2014 proceeds anyway. The only visible symptom is a closeout that unexpectedly demands a Guardian approval, which is three steps removed from the cause.",
  "required_skills": [
    "tdd",
    "code-review"
  ],
  "risk_level": "high",
  "source_file": "/Users/kio.mizutani/shiki/.claude/worktrees/guardian-approval-planning-13e494/.shiki/inbox/P-LOCKGRANT-draft.json",
  "spec_freeze": {
    "amendments": [],
    "approved_by": "mizutani-140",
    "prd": "docs/agents/execution-control.md",
    "source": "Operator instruction 2026-08-05 to work the remaining backlog in the audited recommended order; H is third, after the two zero-conflict items.",
    "status": "frozen"
  },
  "status": "ingested",
  "tasks": [
    {
      "acceptance_checks": [
        "evaluate_lock_grant returns satisfied for a task whose locks list is EMPTY \u2014 the vacuity case, asserted explicitly and documented in the docstring.",
        "A task with an active lock record covering every declared lock dispatches exactly as today.",
        "A missing lock record refuses the dispatch, and the reason names the task and the declared locks.",
        "A record whose state is not active refuses.",
        "A record covering only some declared locks refuses and names the uncovered ones.",
        "An unreadable or malformed record refuses.",
        "dispatchable_task_ids excludes any task the dispatcher would refuse \u2014 the two use the same predicate.",
        "cmd_task_status refuses to move an ungranted task into ready, with the same reason, and succeeds once granted.",
        "Neither dispatch nor a status change ever acquires a lock as a side effect.",
        "The measured 2026-08-04 sequence \u2014 conflicted lock acquire, then dispatch \u2014 is refused with a reason naming the conflicting task.",
        "tests/test_lock_lifecycle.py's cmd_dispatch_check driver agrees with the new predicate.",
        "scripts/test_shiki_runner_claude.sh, test_shiki_daemon_runner.sh, test_shiki_loop_lock_guard.sh, test_shiki_control_plane.sh and test_shiki_goal_loop.sh all pass unchanged.",
        "No file outside the declared locks is changed."
      ],
      "dependencies": [],
      "locks": [
        "path:scripts/shiki_runtime.py",
        "path:scripts/shiki_tasks.py",
        "path:tests/test_lock_lifecycle.py",
        "path:tests/test_dispatch_lock_precondition.py"
      ],
      "non_goals": [
        "A merge-side lock-grant requirement in mergegate_check.py",
        "Changing what a lock conflict means, or how locks are declared or released",
        "Adding a shiki lock release command",
        "Auto-acquiring locks as a side effect of dispatch or of a status change",
        "Changing the loop's decision function",
        "Touching any file outside the declared locks"
      ],
      "risk_level": "high",
      "runtime": "claude-code",
      "scope": "Add a shared predicate \u2014 `evaluate_lock_grant(target, task)` in `scripts/shiki_tasks.py` \u2014 returning whether the task's declared locks are currently granted to IT, plus a reason when they are not. Semantics, stated explicitly in the docstring:\n  - a task whose `locks` list is EMPTY has nothing to grant and is SATISFIED (vacuously). Treating an empty list as ungranted would block every lock-free task;\n  - a task that declares locks is satisfied only when `.shiki/locks/<task_id>.json` exists, is readable, has `state: active`, and its `locks` cover every path the task declares;\n  - anything else \u2014 file missing, unreadable, `state` not active, or a declared lock absent from the record \u2014 is NOT satisfied, and the reason names the task and the specific missing or stale locks.\n\nEnforce it in `scripts/shiki_runtime.py` beside the existing status check in `dispatch_runner_task` (`:522-526`): a task that is not satisfied is refused with that reason, in the same style as the existing refusals. `shiki_runtime.py` already imports from `shiki_tasks`, so no import cycle is introduced. Make `dispatchable_task_ids` (`:291-310`) and the `cmd_runner_execute` status write (`:299`) agree with the same predicate, so a task the dispatcher would refuse is not advertised as dispatchable.\n\nIn `scripts/shiki_tasks.py`, make the failure impossible to miss at its source: `cmd_lock_acquire` (`:946`) must keep returning non-zero on conflict AND must make the ungranted state legible to the next step; and `cmd_task_status` (`:1164`), which today writes `status = \"ready\"` with no lock check, must refuse to move a task INTO `ready` when its declared locks are not granted \u2014 naming the same reason. Do not auto-acquire: dispatch and status must REFUSE, never grant.\n\nAdd `tests/test_dispatch_lock_precondition.py` driving the real functions: a task with an active, covering lock record dispatches; the record missing refuses and the reason names the task and the locks; `state` not active refuses; a record that covers only SOME declared locks refuses and names the uncovered ones; an unreadable/malformed record refuses; a task with an empty `locks` list dispatches (the vacuity case, asserted explicitly); `dispatchable_task_ids` excludes a task the dispatcher would refuse; `cmd_task_status --status ready` refuses for an ungranted task and succeeds once granted; and a conflicted `lock acquire` followed by a dispatch attempt is refused \u2014 the exact sequence measured on 2026-08-04. Extend `tests/test_lock_lifecycle.py` where it drives `cmd_dispatch_check` (`:287`) so the two cannot drift.\n\nREPRODUCIBILITY \u2014 what this task removes, and what to change to get the same result.\n  Symptom: a task is dispatched, implemented, reviewed and merged while its lock grant does not exist. The only visible signal appears at the very end, as a closeout that unexpectedly demands a Guardian approval \u2014 because the ADR 0017 bookkeeping exemption requires the lock to move `active -> released`, and there is no lock to move. The cause is three steps upstream and nothing points at it.\n  Reproduced live 2026-08-04: `shiki lock acquire` was piped through `tail -1`; the visible output was the closing brace, and `\"locks_granted\": false` with five conflict reasons \u2014 naming a superseded task that still held the paths \u2014 scrolled past. `shiki handoff task`, `shiki task status --status ready` and `shiki runner claude` all proceeded.\n  Root cause, verified by reading: `cmd_lock_acquire` (`scripts/shiki_tasks.py:946`) returns 1 at the conflict branch BEFORE `write_json(lock_file, record)` (`:966`) and before the status write (`:982`), so nothing is recorded. `dispatch_runner_task` (`scripts/shiki_runtime.py:512-530`) checks runtime, status, tooling, auth, handoff, worktree and lease \u2014 and never reads `.shiki/locks/`. `cmd_task_status` (`:1164`) writes `ready` with no lock check.\n  Why dispatch-side only: requiring a lock grant at MERGE time would retroactively block reopened work \u2014 `origin/main` holds exactly one active lock record for twelve non-terminal tasks \u2014 and would pull `scripts/mergegate_check.py` plus four shell fixture files into the change.\n  Failure direction matters: a wrong predicate refuses every dispatch and stops all autonomous work, which is worse than the defect. Hence the explicit vacuity rule for empty `locks`, and reasons that always name the task and the specific locks.\n  Reproducible when: the 2026-08-04 sequence \u2014 conflicted acquire, then dispatch \u2014 is refused with a reason naming the conflicting task, and a task with an empty locks list still dispatches.",
      "test_command": "python3 -m unittest discover -s tests && bash scripts/test_shiki_runner_claude.sh && bash scripts/test_shiki_daemon_runner.sh && bash scripts/test_shiki_loop_lock_guard.sh && bash scripts/test_shiki_goal_loop.sh && bash scripts/test_shiki_control_plane.sh && python3 scripts/validate_shiki.py --target .",
      "title": "Make a granted lock a precondition of dispatch"
    }
  ],
  "title": "Refuse to dispatch a task whose locks were never granted"
}
