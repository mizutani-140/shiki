{
  "completion_conditions": [
    "docs/adr/0016 and docs/adr/0017 exist on the default branch with their content unchanged.",
    "A test fails when any `ADR NNNN` cited in scripts/ has no corresponding docs/adr/NNNN-*.md.",
    "The full suite and validate_shiki stay green."
  ],
  "grill_with_docs": {
    "decisions": [
      "Measured: `git cat-file -e origin/main:docs/adr/0016-locks-bound-product-paths.md` and the 0017 equivalent both fail; `git ls-tree origin/main docs/adr/` lists 0001-0015 plus README and template. The commits that authored them (9914d22, 9515998) are NOT reachable from origin/main \u2014 they were written on a coordinator branch and never rode a PR.",
      "Measured citations on origin/main: `scripts/mergegate_check.py` contains 'ADR 0017' nine times; `scripts/guardian_approval_signal.py` contains 'ADR 0016'/'ADR 0017' seven times. `docs/agents/checklists.md`, `docs/agents/decision-control.md`, `CONTEXT.md` and `AGENTS.md` cite neither.",
      "Why it stayed silent: nothing binds a citation to a file. `grep -n 'docs/adr' scripts/validate_shiki.py` returns nothing \u2014 no validator requires a cited ADR to exist. This is the same unbound-pair shape that produced the other silent failures this week, so the fix is the pair binding, not just the two files.",
      "The content is not authored here. Both documents already exist verbatim in the coordinator checkout with their own commit history; this task carries them unchanged. Rewriting a decision record after the fact would misrepresent when the decision was made.",
      "LOCK-SUFFICIENCY CHECK. The two ADR paths are MUST-CHANGE (they are the deliverable). `tests/test_adr_references.py` is MUST-CHANGE (new). Nothing else: `tests/test_install_completeness.py`'s `classify()` returns None for `tests/\u2026` so no TEMPLATE_PATHS entry is needed; `docs/adr/` ships as part of `docs` and needs no manifest change; `scripts/validate_shiki.py` is deliberately NOT locked because the binding lives in a test, not in the validator, and touching validate_shiki would collide with four other backlog items.",
      "Risk is low and honest: no product behaviour changes, no gate moves, no schema changes. Two documents are added and one test asserts a property that already holds once they are. Under `.shiki/guardian-policy.json` (`applies_to_risk: [high, critical]`) neither the Contract PR nor the implementation PR triggers the Guardian gate, so this goal costs zero operator approvals."
    ],
    "source": "Measured 2026-08-04 against origin/main.",
    "status": "complete"
  },
  "id": "P-20260804T094832785739Z-b731b1e2",
  "ingested_at": "2026-08-04T09:48:32+00:00",
  "non_goals": [
    "Rewriting, renumbering or editing the content of either ADR.",
    "Adding a new ADR.",
    "Changing scripts/validate_shiki.py, or any gate, schema or workflow.",
    "Fixing any citation in prose docs \u2014 none cite these ADRs.",
    "Touching any file outside the declared locks."
  ],
  "outcome": "Every `ADR NNNN` a script cites resolves to a real document in the repository. Today two do not: `scripts/mergegate_check.py` cites ADR 0017 nine times and `scripts/guardian_approval_signal.py` cites 0016/0017 seven times, while `docs/adr/` on the default branch stops at 0015. ADR 0017 is the stated basis of the bookkeeping-closeout exemption that has merged seven PRs with zero approvals \u2014 its rationale is currently unreadable to anyone auditing that behaviour, human or CCA.",
  "required_skills": [
    "code-review"
  ],
  "risk_level": "low",
  "source_file": "/Users/kio.mizutani/shiki/.claude/worktrees/guardian-approval-planning-13e494/.shiki/inbox/P-ADRCARRY-draft.json",
  "spec_freeze": {
    "amendments": [],
    "approved_by": "mizutani-140",
    "prd": "docs/adr/README.md",
    "source": "Operator decision 2026-08-04, choosing a small dedicated contract over folding the carry into the SYNCCARRY goal, so a critical review does not gate a documentation move.",
    "status": "frozen"
  },
  "status": "ingested",
  "tasks": [
    {
      "acceptance_checks": [
        "docs/adr/0016-locks-bound-product-paths.md exists and its content is byte-identical to the coordinator copy.",
        "docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md exists and its content is byte-identical to the coordinator copy.",
        "tests/test_adr_references.py asserts every `ADR ####` cited in scripts/*.py resolves to exactly one docs/adr/####-*.md.",
        "The test FAILS when a citation has no matching document \u2014 proved with a fixture, not only by passing today.",
        "The test does NOT require an ADR to be cited.",
        "The failure message names the citing file, the cited number and the missing path.",
        "python3 -m unittest discover -s tests and python3 scripts/validate_shiki.py --target . both pass.",
        "No file outside the declared locks is changed."
      ],
      "dependencies": [],
      "locks": [
        "path:docs/adr/0016-locks-bound-product-paths.md",
        "path:docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md",
        "path:tests/test_adr_references.py"
      ],
      "non_goals": [
        "Rewriting, renumbering or editing either ADR's content",
        "Adding a new ADR",
        "Changing scripts/validate_shiki.py or any gate, schema or workflow",
        "Requiring every ADR to be cited",
        "Touching any file outside the declared locks"
      ],
      "risk_level": "low",
      "runtime": "claude-code",
      "scope": "Add `docs/adr/0016-locks-bound-product-paths.md` (233 lines) and `docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md` (129 lines). Both already exist verbatim in the coordinator checkout this session runs from; copy them unchanged. Do not rewrite, reformat, renumber or update them \u2014 a decision record edited after the fact misrepresents when the decision was made.\n\nAdd `tests/test_adr_references.py` binding citations to files: scan every `scripts/*.py` (and, if cheap, every `docs/**/*.md` and the root `*.md` governance files) for the pattern `ADR ####`, and assert each cited number resolves to exactly one `docs/adr/####-*.md`. The failure message must name the citing file, the cited number, and the missing path. Assert the converse is NOT required \u2014 an ADR may exist without being cited.\n\nREPRODUCIBILITY \u2014 what this task removes, and what to change to get the same result.\n  Symptom: an operator or a CCA reads `is_bookkeeping_closeout` in `scripts/mergegate_check.py`, sees it justified by 'ADR 0017' nine times, goes to `docs/adr/` and finds the numbering stops at 0015. The stated basis of the exemption that merged seven PRs with zero Guardian approvals cannot be read.\n  Measured on origin/main 2026-08-04: `git cat-file -e origin/main:docs/adr/0017-bookkeeping-closeouts-do-not-inherit-risk.md` fails; so does 0016. `git merge-base --is-ancestor 9914d22 origin/main` fails \u2014 the authoring commits are unreachable from main. Meanwhile `scripts/mergegate_check.py` cites ADR 0017 nine times and `scripts/guardian_approval_signal.py` cites 0016/0017 seven times.\n  Root cause: the documents were written on a coordinator branch during the work they describe, and no task ever declared `docs/adr/\u2026` in its locks, so nothing carried them. ADR 0015 reached main only because a Contract PR carries its Goal's source plan; 0016 and 0017 had no equivalent route.\n  Why it was silent: `grep -n 'docs/adr' scripts/validate_shiki.py` returns nothing. No validator requires a cited ADR to exist, so the citations and the directory drifted with no signal. This is the same unbound-pair shape behind the other silent failures this week \u2014 hence the test, not just the two files.\n  Reproducible when: both files are on the default branch and the new test fails if a script cites an ADR number with no matching document.",
      "test_command": "python3 -m unittest discover -s tests && python3 scripts/validate_shiki.py --target .",
      "title": "Carry ADR 0016 and 0017 and bind ADR citations to their files"
    }
  ],
  "title": "Carry the two ADRs the code already cites, and bind citations to files"
}
